## R CMD check results

0 errors | 0 warnings | 0 notes

### 2.2.1 -> 2.2.2

I have shortened the test time on CRAN from 549 to ~240 seconds, and also shortened the example times for `export_plot()`, `as.data.frame.ensemble_stockflow()`, `plot.simulate_stockflow()`, `plot.verify_stockflow()`.
